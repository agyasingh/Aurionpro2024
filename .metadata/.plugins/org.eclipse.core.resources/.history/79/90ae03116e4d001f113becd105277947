package com.aurionpro.test;

import java.util.Arrays;
import java.util.Collections;
import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.IntStream;
import java.util.stream.Stream;

public class StringsTest {
public static void main(String[] args) {
	String[] names= {"aagya", "sampada" ,"missraina", "poonam"};
	
  
    List<String> sortedNames = Arrays.stream(names)
    		                  .limit(3)
    		                  .sorted()
    		                  .collect(Collectors.toList());
    

    sortedNames.forEach(System.out::println);
    
    Collections.reverse(sortedNames);
    
    sortedNames.forEach(System.out::println);
    
//    List<String> sortedNamesStreamCharacters = sortedNames.stream()
//            .flatMapToInt(CharSequence::codePoints)  // Convert each name to a stream of character code points
//            .mapToObj(c -> String.valueOf((char) c)) // Convert each code point to a String character
//            .limit(3)  // Limit to the first 3 characters
//            .collect(Collectors.toList()); // Collect characters into a list
  
//  sortedNameStream.forEach((sortedNameStreamCharacters)->System.out.println(sortedNameSteamCharacters));
//    		  .mapToObj(c -> String.valueOf((char) c)).limit(3).collect(Collectors.asList());
//    
    
    sortedNamesStreamCharacters.forEach(System.out::println);
}
}
