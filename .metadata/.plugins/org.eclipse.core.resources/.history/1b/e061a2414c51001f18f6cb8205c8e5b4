package com.aurionpro.model;

public class EmailNotifier implements INotifier {
    @Override
    public void notify(Account account, double amount) {
        System.out.println("Email: An amount of " + amount + "Current balance: " + account.getBalance());
    }
}

